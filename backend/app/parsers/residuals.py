"""Solver log parsing: residuals, force coefficients, Courant numbers.

TODO(phase-1): line parsers feeding the run WebSocket stream; divergence
auto-detection (residual spike heuristics) triggering stop + diagnosis hint.
"""
