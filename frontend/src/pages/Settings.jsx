import { Placeholder } from "@/components/Placeholder";

export function Settings() {
  return (
    <Placeholder
      title="Settings"
      description="OpenFOAM image selection, data directory, units preferences, remote runners (later)."
      phase="Phase 1"
    />
  );
}
